# Notion internal integration token — used by scripts/fetch_elements.py (read-only sync)
NOTION_TOKEN=

# Anthropic API key — used by server.js for the photo analyzer (POST /api/analyze)
ANTHROPIC_API_KEY=
